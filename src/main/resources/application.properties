# Database Configuration - Using MySQL database
spring.datasource.url=jdbc:mysql://localhost:3306/tiktok_reels?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Jaimaiki12345#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# MySQL Connection Pool
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5

# File Upload Configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# JWT Configuration
jwt.secret=tiktokReelsSecretKey123456789012345678901234567890
jwt.expiration=86400000

# AWS S3 Configuration (replace with your own values)
aws.s3.bucket-name=tiktok-reels-videos
aws.region=us-east-1

# Server Configuration
server.port=8081
